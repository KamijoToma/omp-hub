{
  "type": "module",
  "name": "omp-hub-web",
  "version": "0.1.0",
  "private": true,
  "description": "omp hub web UI: machines, sessions, and the vendored collab guest client",
  "license": "MIT",
  "scripts": {
    "dev": "bun ./index.html",
    "build": "rm -rf dist && bun build ./index.html --outdir=dist --minify --public-path=/ --entry-naming=[hash].[ext] --chunk-naming=[hash].[ext] --asset-naming=[hash].[ext] && mv dist/*.html dist/index.html && cp -R public/. dist/",
    "test": "bun test"
  },
  "dependencies": {
    "lucide-react": "^1.17.0",
    "marked": "^18.0.5",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3"
  },
  "engines": {
    "bun": ">=1.3.14"
  }
}
